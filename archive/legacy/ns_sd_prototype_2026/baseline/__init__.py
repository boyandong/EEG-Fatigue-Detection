"""
Baseline 模型包。

当前内容：
- `run_lstm_baseline.py`：调用 `train_lstm_fatigue.main()` 作为 LSTM 基线实验入口。

后续可以在此处增加：
- LOO 协议下的 LSTM 入口（例如 `run_lstm_baseline_loo.py`）
- 其它模型的 baseline 脚本（如 CNN / ViT / Transformer 等）
"""

